#!/bin/bash
class="$1"
prog="$2"
count=`ps aux | grep -c $prog`
echo $count
if [ $count -eq "3" ]; then
	exec "$prog"
else
	i3-msg "[class=$class] focus"
	i3-msg "[title=$class] focus"
fi
# usage
#bindsym $mod+e exec ~/appjump.sh 'Emacs' emacs &
